[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci", "cd backend && npm ci"]

[phases.build]
cmds = ["npm run build", "cd backend && npm run build:server"]

[start]
cmd = "cd backend && node dist/index.js"
